using System;

namespace CurrentDateTime
{
    class CurrentDateTime
    {
        static void Main()
        {
            DateTime now = DateTime.Now; // DateTime is used to store a date and DateTime.Now shows the current date.
            Console.WriteLine(now); // The console prints the value of "now".
        }
    }
}
